const Tagline = ({ size }) => {
  return (
    <h1 className={`text-white font-semibold ${size}`}>
      List What <span className="text-secondary">U</span> Gonna{" "}
      <span className="text-secondary">W</span>atch
    </h1>
  );
};

export default Tagline;
